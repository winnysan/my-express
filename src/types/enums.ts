export enum NodeEnv {
  PROD = 'production',
  DEV = 'development',
}

export enum Domain {
  COM = 'com',
  SK = 'sk',
}

export enum Role {
  ADMIN = 'admin',
  USER = 'user',
}

export enum CategoryAction {
  ADD_FIRST = 'add-first',
  ADD = 'add',
  ADD_NESTED = 'add-nested',
  DELETE = 'delete',
  UP = 'up',
  DOWN = 'down',
  RENAME = 'rename',
  SET_LOCALE = 'set-locale',
}
